ncurses-5.7-built-for-android.tar.gz:
  I started from stock ncurses-5.7 and hacked on it until it compiled using
  the agcc.pl script and the NDK, then tarred up the whole directory and
  dumped it here.  Oddly, vim seemed to compile just fine even without 
  knowing where to find the #includes.
